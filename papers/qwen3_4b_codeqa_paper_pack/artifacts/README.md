# Artifacts README

这个目录用于保存“随包走”的原始结果快照。

当前包含：

- `canonical_single_seed/metrics_table.csv`
- `canonical_single_seed/task_metrics.csv`
- `canonical_single_seed/key_task_summary.csv`
- `canonical_single_seed/error_cases.jsonl`

建议用途：

- 正文写作时，以 `evidence/` 下的冻结页为准
- 核查原始数字和案例时，再回到这里
