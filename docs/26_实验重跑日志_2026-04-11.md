# 26 实验重跑日志（2026-04-11）

## 1. 当前状态

- 主评测数据已本地就绪：`data/main_eval/longbench_3tasks_test.jsonl`
- 快速优化集已本地就绪：`data/main_eval/longbench_3tasks_dev100.jsonl`
- 风险监测集已本地就绪：`data/main_eval/longbench_3tasks_holdout100.jsonl`
- `train_ext` 当前未完整恢复；原因是本机到 Hugging Face 网络不稳定
- 本轮重跑优先采用 `sample` 检索作用域，在样本内构建索引，不依赖全量 `train_ext`

## 2. 运行时约束

- 本地模型后端：`ollama`，模型 `qwen3:8b`
- 当前机器无法稳定访问 Hugging Face 远端接口，因此不执行 `prepare_remote_strict.sh`
- 为降低每轮样本内检索初始化开销，已在 `retrieval/index_faiss.py` 增加：
  - 本地缓存的 `sentence-transformers/all-MiniLM-L6-v2` 优先加载
  - `SentenceTransformer` 进程内复用

## 3. 本轮策略

- 先在 `dev100` 上跑完整四路对照：
  - `baseline_rag`
  - `baseline_budget_matched`
  - `baseline_single_round_strong`
  - `edge_agent`
- 每轮结束后读取评测报告与样本级结果
- 只做“有明确归因”的小步调参，然后继续下一轮
- 配置稳定后，再决定是否推进更大规模 `canonical` 重跑

## 4. Round Log

### Round 1

- 数据集：`data/main_eval/longbench_3tasks_dev100.jsonl`
- 状态：完成
- 运行前修复：
  - `retrieval/index_faiss.py` 增加本地缓存 `all-MiniLM-L6-v2` 的优先加载与进程内复用
  - 修复本机 `ollama` 默认模型目录缺失完整 `qwen3:8b` blob 的问题，恢复 real backend
  - `run_baseline_rag.py` 中 baseline 异常 fallback 的 `backend_mode` 统一写为 `unknown`
- 结果文件：
  - `results/rerun_round1_dev100_baseline.jsonl`
  - `results/rerun_round1_dev100_budget.jsonl`
  - `results/rerun_round1_dev100_single_round.jsonl`
  - `results/rerun_round1_dev100_agent.jsonl`
- 报告目录：`report/rerun_round1_dev100`
- 总体指标：
  - `baseline_rag`: `F1=0.3787`, `P95=29.3s`
  - `baseline_budget_matched`: `F1=0.3534`, `P95=25.2s`
  - `baseline_single_round_strong`: `F1=0.4225`, `P95=33.3s`
  - `edge_agent`: `F1=0.4485`, `P95=60.3s`
- 任务分解：
  - `edge_agent` 在 `multi_doc_qa` 最强：`F1=0.5157`
  - `edge_agent` 在 `code_qa` 未超过最强单轮基线：`0.3692 < 0.3795`
  - `single_doc_qa` 四路都偏弱，`edge_agent=0.2040`
- 样本级归因：
  - `code_qa` 失败样本中，模型常把“下一行代码”答成解释段落
  - `single_doc_qa` 失败样本中，模型常给出过长答案，而不是短 span / yes-no
  - `multi_doc_qa` 的优势是真实存在的，很多样本从部分正确提升到直接命中
- Round 1 结论：
  - 当前默认 `agent` 已经是 `dev100` 上的最好总体配置
  - Round 2 应聚焦于 `code_qa` 代码行抽取和 `single_doc_qa` 短答案压缩
  - 不改动 `multi_doc_qa` 的主搜索流程

### Round 2

- 状态：完成
- 配置 / 代码改动：
  - `agent/edge_reasoning_agent.py`
    - 新增 `_clean_code_answer()`，优先抽取单行代码式答案
    - `entity_short` 增加 yes/no 归一化
    - code 任务的 prompt note 增加 `one_line_only_no_explanation`
  - `configs/agent.yaml`
    - `answer_postprocess.single_doc_compact: true`
    - `single_doc_qa.answer_type_policy.enable: true`
- 结果文件：
  - `results/rerun_round2_dev100_agent.jsonl`
- 报告目录：`report/rerun_round2_dev100`
- 与 Round 1 agent 对比：
  - overall `F1: 0.4485 -> 0.4451`
  - `EM: 0.22 -> 0.18`
  - `P95 latency: 60.3s -> 59.9s`
  - `code_qa F1: 0.3692 -> 0.4690`
  - `multi_doc_qa F1: 0.5157 -> 0.4620`
  - `single_doc_qa F1: 0.2040 -> 0.1888`
- probe 样本观察：
  - `code_qa` 和部分 `single_doc_qa` 的确明显改善
  - 但全量 `dev100` 上，`multi_doc_qa` 出现明显回撤
- Round 2 结论：
  - 该改动适合保留为“代码题增强分支”，不应直接替换主线默认 agent
  - 当前 `dev100` 主线最佳仍是 Round 1 默认 agent
  - 若继续推进，应将 Round 2 的代码行清洗能力拆成更窄的可控 ablation，而不是整包升主线

### Round 3

- 状态：完成
- 目标：把 Round 2 拆成更窄的 `code_only` 分支，只保留代码题答案清洗，不再同时改动 `single_doc_qa`
- 配置：
  - `configs/agent_code_only_candidate.yaml`
  - 关键差异：
    - `answer_postprocess.code_compact: true`
    - `answer_postprocess.single_doc_compact: false`
    - `single_doc_qa.answer_type_policy.enable: false`
- 结果文件：
  - `results/rerun_holdout100_code_only_agent.jsonl`
  - `results/rerun_dev100_code_only_agent.jsonl`
- 报告目录：
  - `report/rerun_holdout100_agent_variants`
  - `report/rerun_dev100_code_only_compare`
- 与 Round 1 mainline 对比：
  - `dev100 overall F1: 0.4485 -> 0.4490`
  - `dev100 code_qa F1: 0.3692 -> 0.4714`
  - `dev100 multi_doc_qa F1: 0.5157 -> 0.4647`
  - `dev100 single_doc_qa F1: 0.2040 -> 0.2116`
  - `dev100 P95 latency: 60.3s -> 49.3s`
  - `holdout100 overall F1: 0.4503 -> 0.4677`
  - `holdout100 code_qa F1: 0.4012 -> 0.4540`
  - `holdout100 multi_doc_qa F1: 0.4832 -> 0.4823`
  - `holdout100 single_doc_qa F1: 0.3769 -> 0.3991`
  - `holdout100 P95 latency: 47.4s -> 50.2s`
- Round 3 结论：
  - `code_only` 的净收益是真实存在的，尤其体现在 `code_qa`
  - 该分支在 `holdout100` 上优于 mainline，但在 `dev100` 上仍伴随明显的 `multi_doc_qa` 回撤
  - 下一步应尝试只补 `multi_doc` 的答案压缩，而不再改动主搜索流程

### Round 4

- 状态：完成
- 目标：在 `code_only` 基础上，仅将 `multi_doc_qa` 的后处理从 `conservative` 调到 `aggressive`
- 配置：
  - `configs/agent_code_multi_doc_compact_candidate.yaml`
  - 关键差异：
    - `answer_postprocess.code_compact: true`
    - `answer_postprocess.task_modes.multi_doc_qa: aggressive`
    - `single_doc_qa` 仍保持 Round 1 口径
- 结果文件：
  - `results/rerun_dev100_code_multi_doc_compact_agent.jsonl`
  - `results/rerun_holdout100_code_multi_doc_compact_agent.jsonl`
- 报告目录：
  - `report/rerun_dev100_variant_compare_round3`
  - `report/rerun_holdout100_variant_compare_round3`
- 与 Round 1 mainline 对比：
  - `dev100 overall F1: 0.4485 -> 0.4770`
  - `dev100 code_qa F1: 0.3692 -> 0.4744`
  - `dev100 multi_doc_qa F1: 0.5157 -> 0.5082`
  - `dev100 single_doc_qa F1: 0.2040 -> 0.2116`
  - `dev100 P95 latency: 60.3s -> 53.6s`
  - `holdout100 overall F1: 0.4503 -> 0.5081`
  - `holdout100 code_qa F1: 0.4012 -> 0.4562`
  - `holdout100 multi_doc_qa F1: 0.4832 -> 0.5459`
  - `holdout100 single_doc_qa F1: 0.3769 -> 0.4035`
  - `holdout100 P95 latency: 47.4s -> 46.1s`
- 与 Round 3 `code_only` 对比：
  - `dev100 overall F1: 0.4490 -> 0.4770`
  - `dev100 multi_doc_qa F1: 0.4647 -> 0.5082`
  - `holdout100 overall F1: 0.4677 -> 0.5081`
  - `holdout100 multi_doc_qa F1: 0.4823 -> 0.5459`
  - `holdout100 P95 latency: 50.2s -> 46.1s`
- Round 4 结论：
  - 这是一条目前最强、且跨 `dev100` / `holdout100` 都成立的改进分支
  - 它不是依赖更多检索步数或更长输出，而是通过更稳定的 `code_qa` 清洗和 `multi_doc_qa` 压缩获得净收益
  - 该配置已达到“下一版默认 mainline 候选”的标准

### Round 5

- 状态：完成
- 目标：在真正启动 `canonical(2550)` 之前，先用主评测分布上的 30 条分层样本做 quick gate
- 数据集：
  - `data/main_eval/longbench_3tasks_quickgate30.jsonl`
  - 组成：`single_doc_qa=10`，`multi_doc_qa=10`，`code_qa=10`
- 配置：
  - `configs/agent.yaml`
  - 已同步为 Round 4 口径
- 结果文件：
  - `results/rerun_quickgate30_agent.yaml.jsonl`
- 报告目录：
  - `report/rerun_quickgate30_agent_yaml`
- quick gate 检查结果：
  - `backend_mode=real`: `30/30`
  - `error_count`: `0`
  - overall `F1=0.3827`
  - `single_doc_qa F1=0.5461`
  - `multi_doc_qa F1=0.3326`
  - `code_qa F1=0.2693`
  - `P95 latency=57.7s`
  - `OOM=0`
- Round 5 结论：
  - 新主线配置在 canonical 分布上可稳定跑通，`real backend` 与显存占用均正常
  - 由于 quick gate 只有 30 条且各任务均衡采样，分数不用于替代正式主结论
  - 该轮的价值在于确认：可以进入下一阶段的 `canonical(2550)` 全量正式重跑

## 5. 当前决策

- 默认主线候选：提升为 Round 4 `code_only + multi_doc_compact`
- `configs/agent.yaml` 已同步到 Round 4 口径，作为后续正式实验默认配置
- Round 5 quick gate 已通过，可作为启动 `canonical(2550)` 的最后检查
- `Round 2` 保留为失败案例：说明“代码增强 + single_doc 压缩”整包替换会伤害 `multi_doc_qa`
- `Round 3` 保留为中间证据：证明 `code_compact` 单独引入时已有净收益，但不足以成为最终主线
- 下一步应考虑启动 `canonical(2550)` 全量重跑
  - 原因是当前候选已在 `dev100` 与 `holdout100` 上同时优于旧主线
  - quick gate 也已确认 `backend_mode=real`、`OOM=0`、耗时无异常
  - 此时放大到 canonical 才有足够信息增益

## 6. 下一步建议

- 方案 A：以当前 `configs/agent.yaml` 为主线，启动 `canonical(2550)` 全量重跑
- 方案 B：按 Round 5 的 quick gate 耗时估算全量运行窗口，并预留连续运行时段
- 方案 C：待 `train_ext` 恢复后，再进入真正的 global-index / student 分支
