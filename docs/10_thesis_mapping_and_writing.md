# 10. 论文写作映射（v4）

## 10.1 你要强调的两点贡献（推荐措辞）
- Model-Aware Eviction（Thinking-Aware Budgeting）：利用 `<think>` 抽取关键事实压缩工作记忆，在 8GB 显存下稳定推理。
- Test-time Retrieval：把 test-time compute 转化为 test-time retrieval，以多轮检索替代更大模型参数。

## 10.2 章节映射
- 第 4 章（实验设置）：参考 docs/01、docs/05、configs、metadata.json
- 第 5 章（结果）：report/metrics_table.csv + 图（docs/13）
- 失败案例：trace（docs/16_trace_example.md）

## 10.3 引用
- 用 references.bib；访问日期写明（Accessed: YYYY-MM-DD）
