# Core (Python 3.9 compatible)
numpy>=1.24.0
pandas>=1.5.0
matplotlib>=3.7.0
tqdm>=4.65.0
PyYAML>=6.0
requests>=2.31.0
jsonschema>=4.17.0

# Retrieval / runtime optional accelerators
sentence-transformers>=2.6.0
faiss-cpu>=1.7.4; platform_system != "Darwin"
chromadb>=0.4.0
pynvml>=11.5.0

# Training stack (optional; scripts support graceful fallback)
torch>=2.1.0
transformers>=4.38.0
datasets>=2.14.0
accelerate>=0.27.0
peft>=0.10.0
trl>=0.7.0
bitsandbytes>=0.41.0; platform_system == "Linux"
